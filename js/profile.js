
// الانتظار حتى يتم تحميل كافة عناصر واجهة الصفحة (HTML) لضمان أن الكود سيعمل دون أخطاء
document.addEventListener('DOMContentLoaded', function() 
{
    // جلب عنصر إدخال الملفات (input) الذي يسمح للمستخدم باختيار صورة من جهازه
    const avatarUpload = document.getElementById('avatarUpload');

    // جلب عنصر الصورة (img) الذي سيعرض الأفاتار أو الملف الشخصي في الصفحة
    const profileAvatar = document.getElementById('profileAvatar');

// التحقق من أن العناصر (input و img) موجودة فعلياً في الصفحة قبل البدء بالعمل
    if (avatarUpload && profileAvatar) {

        // إضافة مراقب أحداث يعمل فور قيام المستخدم باختيار ملف جديد (تغيير القيمة)
        avatarUpload.addEventListener('change', function(e) {

            // استخراج الملف الأول فقط من قائمة الملفات التي اختارها المستخدم
            const file = e.target.files[0];

            // التأكد من أن المستخدم اختار ملفاً بالفعل ولم يقم بإلغاء الاختيار
            if (file) {
// إنشاء كائن FileReader الذي يسمح للمتصفح بقراءة الملفات المحلية
                const reader = new FileReader();
                // تعريف وظيفة يتم تنفيذها بمجرد انتهاء المتصفح من قراءة محتوى الملف بنجاح
                reader.onload = function(event) {
                    // تحديث مصدر الصورة (src) لتصبح هي البيانات التي تمت قراءتها من الجهاز
                    profileAvatar.src = event.target.result;
                };
                // البدء الفعلي في قراءة الملف وتحويله إلى صيغة نصية (Data URL) قابلة للعرض كصورة
                reader.readAsDataURL(file);
            }
        });
    }

    // زر تسجيل الخروج
    // البحث عن زر تسجيل الخروج باستخدام الكلاس الخاص به (btn-outline)
    const logoutBtn = document.querySelector('.btn-outline');
    // التأكد من وجود زر تسجيل الخروج في الصفحة لتجنب الأخطاء البرمجية
    if (logoutBtn) {
        // إضافة مراقب أحداث ينتظر قيام المستخدم بالضغط (Click) على الزر
        logoutBtn.addEventListener('click', function() {

            alert("👋 تم تسجيل الخروج!");
            // تغيير عنوان الصفحة وتوجيه المستخدم تلقائياً إلى الصفحة الرئيسية (index.html)
            window.location.href = 'login.html';
        });
    }
});